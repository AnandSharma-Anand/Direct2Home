/*
import 'package:erp_school/common/appcolors.dart';
import 'package:flutter/material.dart';
import 'package:shimmer/shimmer.dart';

class CustomShimmerWidget<T> extends StatelessWidget {

  final Widget? child;

  const CustomShimmerWidget({
    Key? key,
     this.child
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return new Padding(
        padding: new EdgeInsets.only(top: 5.0,left: 20),
        child: Shimmer.fromColors(
          baseColor: AppColors.light_grey.withOpacity(0.5),
          highlightColor: AppColors.header_color,
          child: child!
        )
    );
  }
  }*/
